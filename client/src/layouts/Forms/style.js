import styled from "styled-components";

const Form = styled.form`
  width: 100%;
  margin: 30px 80px;
  & > * {
    width: 100%;
    margin-bottom: 10px;
  }
`;

const FormWrapper = styled.div``;

export default Form;
